<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f1f1f1;
      font-family: 'Arial', sans-serif;
      overflow-x: hidden;
      position: relative;
    }

    .background-design {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
      background: linear-gradient(135deg, rgba(0, 150, 136, 0.2), rgba(0, 150, 136, 0.15));
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .background-design svg {
      position: absolute;
      width: 500px;
      height: 500px;
      fill: none;
      stroke: rgba(0, 150, 136, 0.5);
      stroke-width: 2;
    }

    
    .clock {
      position: relative;
      width: 500px;
      height: 500px;
      border-radius: 50%;
      background: #ffffff;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
      border: 8px solid #009688;
    }

    .clock .hour,
    .clock .minute,
    .clock .second {
      position: absolute;
      bottom: 50%;
      left: 50%;
      transform-origin: 50% 100%;
      transition-timing-function: linear;
    }

    .clock .hour {
      width: 12px;
      height: 180px;
      background: #009688;
      transform: rotate(90deg);
      animation: rotateHour 1440s infinite linear; 
    }

    .clock .minute {
      width: 9px;
      height: 220px;
      background: #00796b;
      transform: rotate(180deg);
      animation: rotateMinute 180s infinite linear; 
    }

    .clock .second {
      width: 4px;
      height: 240px; 
      background: red;
      transform: rotate(270deg);
      animation: rotateSecond 10s infinite linear; 
    }

    @keyframes rotateHour {
      0% {
        transform: rotate(90deg);
      }
      100% {
        transform: rotate(450deg);
      }
    }

    @keyframes rotateMinute {
      0% {
        transform: rotate(180deg);
      }
      100% {
        transform: rotate(540deg);
      }
    }

    @keyframes rotateSecond {
      0% {
        transform: rotate(270deg);
      }
      100% {
        transform: rotate(630deg);
      }
    }

    h1 {
      font-size: 4rem;
      font-weight: 900;
      color: #009688;
      text-shadow: 0 4px 10px rgba(0, 150, 136, 0.3);
      animation: fadeInDown 1s ease;
    }

    p {
      font-size: 1.1rem;
      font-weight: 300;
      line-height: 1.5;
      max-width: 450px;
      color: #004d40;
      margin: 1rem auto;
    }

    .btn-primary {
      background-color: #009688;
      border-color: #00796b;
      font-size: 1.2rem;
      padding: 1rem 2.5rem;
      border-radius: 50px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .btn-outline-dark {
      color: #009688;
      border: 2px solid #009688;
      font-size: 1.2rem;
      padding: 1rem 2.5rem;
      border-radius: 50px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .btn-primary:hover,
    .btn-outline-dark:hover {
      transform: scale(1.05);
      box-shadow: 0 8px 20px rgba(0, 150, 136, 0.3);
    }

    footer p {
      font-size: 0.9rem;
      color: #004d40;
      margin-top: 2rem;
    }

    @keyframes fadeInDown {
      from {
        opacity: 0;
        transform: translateY(-50px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>
  <div class="background-design">
    <div class="clock">
      <div class="hour"></div>
      <div class="minute"></div>
      <div class="second"></div>
    </div>
  </div>

  <div class="container vh-100 d-flex flex-column justify-content-center align-items-center text-center">
    <h1 class="fw-bold">TimeSync</h1>
    <p>Your all-in-one solution for synchronized time management.</p>

    <div>
      <a href="/register" class="btn btn-primary mx-2">Register</a>
      <a href="/login" class="btn btn-outline-dark mx-2">Login</a>
    </div>

    <footer>
      <p>&copy; 2024 TimeSync. All rights reserved.</p>
    </footer>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
