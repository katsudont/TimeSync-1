<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Assign Shift - {{department.DepartmentName}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container my-5">
    <h1>Assign Shift to {{department.DepartmentName}}</h1>
    
    <form method="POST">
      <div class="mb-3">
        <label for="shiftId" class="form-label">Select Shift</label>
        <select name="shiftId" id="shiftId" class="form-control">
          <option value="">Select a shift</option>
          {{#shifts}}
            <option value="{{ID}}">{{ID}}: {{TimeIn}} - {{TimeOut}}</option>
          {{/shifts}}
        </select>
      </div>
      <button type="submit" class="btn btn-primary">Assign Shift</button>
    </form>

    <a href="/department" class="btn btn-link mt-3">Back to Department List</a>
  </div>
</body>
</html>
